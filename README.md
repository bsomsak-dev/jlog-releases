# jlog releases

Install

```sh
curl -fsSL https://raw.githubusercontent.com/bsomsak-dev/jlog-releases/main/install.sh | sh
```

The installer downloads the matching Playwright Chromium runtime. Then run:

```sh
jlog setup
jlog doctor
```
